# Backend API URL
VITE_API_URL=http://localhost:3000/api

# WebSocket URL
VITE_WS_URL=http://localhost:3000

# Dev-only: bypass auth and use admin permissions (never enable in production)
DEV_AUTH_BYPASS=true
